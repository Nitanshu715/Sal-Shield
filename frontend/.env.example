# SAL-SHIELD Frontend Environment Variables
# Copy to .env.local for local dev
# Copy to .env.production for Vercel

# Point to your Render backend URL after deployment
VITE_API_URL=https://sal-shield-api.onrender.com/api

# For local dev, leave commented out (proxy handles it)
# VITE_API_URL=http://localhost:5000/api
